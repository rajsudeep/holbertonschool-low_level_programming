#include <stdio.h>

/**
 * main - Print programming analogy message
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char analStr[100] = "\"Programming is like building a multilingual puzzle";
	puts(analStr);
	return (0);
}
