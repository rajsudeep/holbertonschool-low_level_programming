#include "holberton.h"

/**
 * _strlen - finds and returns length of string
 *
 * @s: string value
 *
 * Return: length of string
 */
int _strlen(char *s)
{
	int length;

	for (length = 0; s[length] != '\0'; length++)
	{
	}

	return (length);
}
